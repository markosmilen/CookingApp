<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.ShoppingListActivity"
    android:background="@color/primary_background"
    android:fitsSystemWindows="true"
    android:orientation="vertical">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/shoppingRecipes_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:fitsSystemWindows="true"
        android:background="@color/colorWhite"
        android:theme="@style/AppTheme" />

    <!-- Layout in case of no items-->
    <RelativeLayout
        android:id="@+id/no_shoppingList_items_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/shoppingRecipes_toolbar">

        <ImageButton
            android:id="@+id/add_button"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:background="@drawable/ic_add"
            android:layout_centerInParent="true"
            />

        <TextView
            android:id="@+id/add_to_collection"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:layout_below="@+id/add_button"
            android:text="Add to this Collection"
            android:padding="10dp"
            android:textSize="20sp"/>

        <TextView
            android:id="@+id/empty_collection"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="This collection is currently empty :("
            android:textColor="@color/color_primary"
            android:layout_below="@id/add_to_collection"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="DISCOVER THE 360K+ RECIPES WE HAVE"
            android:layout_below="@id/empty_collection"
            android:gravity="center"
            android:padding="10dp"
            />

    </RelativeLayout>

    <!-- Layout in case of  items present-->
    <RelativeLayout
        android:id="@+id/shoppinglist_with_items_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/shoppingRecipes_toolbar">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/shoppingListRecipes_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            />

    </RelativeLayout>

</RelativeLayout>